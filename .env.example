# Port the server listens on
PORT=3000

# Runtime environment: development | production | test
NODE_ENV=development

# Comma-separated list of allowed CORS origins (leave empty to allow none)
CORS_ORIGINS=http://localhost:3000
